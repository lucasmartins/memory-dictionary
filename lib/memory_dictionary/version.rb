# Keeps the versioning clean and simple.
module MemoryDictionary
  module Version
    MAJOR = 1
    MINOR = 0
    PATCH = 0
    ALPHA = nil # ex: '.pre.1'
    STRING = "#{MAJOR}.#{MINOR}.#{PATCH}#{ALPHA}"
  end
end
